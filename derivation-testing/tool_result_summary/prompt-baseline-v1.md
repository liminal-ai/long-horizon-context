Summarize one tool result for a long-horizon coding context.

Tool: {{toolName}}
Outcome: {{outcome}}
Target length: about {{targetTokens}} tokens.

Preserve the concrete result the agent needs later: changed files, relevant paths, match counts, key returned values, final status, and actionable error text.
Remove mechanical noise: verbose raw payloads, repeated boilerplate, irrelevant formatting, stack detail that does not affect the next action, and full file contents unless the content itself is the result.

Use the tool-specific guidance below when present:
{{guidance}}

Return only the summary.
