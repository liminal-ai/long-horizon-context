You are the IMPLEMENTOR for Wave 3 fix round 1 of the lhc-py Phase 1 port. Sol's audit came back FAIL. Same contract: /srv/work/long-horizon-context/docs/lhc-py-port-phase1-brief.md, branch lhc-py-port, no commits/pushes, nothing outside packages/lhc-py/. Fix ALL findings, re-run the gate, report per finding.

FINDINGS (blockers unless noted):

1. src/lhc/threads/internal/create.py:165 — `_validate_thread_file` success in TS is exactly `{ok: true}` (no value). Don't reuse OpOk[Literal[True]]; declare the exact success variant (a small frozen dataclass with only the `ok: Literal[True]` discriminator, per the OpOk/OpErr pattern) + Union with OpErr.
2. src/lhc/intake_stream/internal/validate.py:47 — ORCHESTRATOR RULING on the Effect Schema surface (follow exactly): (a) `_DECODE_OPTIONS` ports VERBATIM as a private dict constant {"onExcessProperty": "error", "errors": "first"}; (b) every `Schema.Struct` constant (ThreadRefSchema, EventEnvelopeSchema, TextPayloadSchema, ModelChangePayloadSchema, ThinkingLevelChangePayloadSchema, ToolCallPayloadSchema, ToolResultPayloadSchema) becomes a private closed TypedDict declaration with verbatim camelCase keys (these are decoded-JSON data shapes) mirroring required/optional exactly, with `NOTE (Phase 2):` markers for non-representable semantics (minLength(1), onExcessProperty closedness, union decode order); (c) `_NonEmptyString` gets a module-level `NOTE (Phase 2):` marker (no Python equivalent — do not invent a DSL); (d) decode helpers keep full signatures typed against these declarations — no bare `object` where a closed alias exists; bodies stay `raise NotImplementedError`; (e) `_first_issue`/ParseError: type the parameter as the closest faithful stand-in with a NOTE, not `object` silently.
3. src/lhc/messages/__init__.py:33 — seam types must be exact even while partial: TS exports `Block` (rename MessageBlock → Block); `BlockType | str` → the closed `BlockType` union; un-widen `MessageRecord.kind` / `MessageCreated.kind`; make MessageRecord fields at lines 43–48 required (no defaults) exactly as TS.
4. src/lhc/turns/__init__.py:21 — `turn_order` and `opened_at_event_order` are required in TS → remove Optional/defaults. Line ~53: `RecordedTurnEvent.event_kind` → closed `EventKind`, not `EventKind | str`.
5. tests/fixtures/__init__.py:69 — re-export the full lifecycle set the TS fixture index exports: create_lifecycle_sdk, DELETE_TARGET, EDIT_TARGET, LifecycleCheckpoint, LifecycleOptions, LifecyclePhases (add to __all__).
6. tests/test_lifecycle.py:33 — TS runs ONE async beforeAll baseline shared by synchronous checkpoint tests, with Date frozen at 2026-06-12T00:00:00.000Z. Translate: module-scoped async pytest fixture (scope="module") computing the shared baseline once + clock control that freezes Date only, exactly as the TS fixture does.
7. tests/test_lifecycle.py:238 — replace json.dumps(..., default=str) with a recursive dataclass→JSON-compatible conversion (dataclasses.asdict or equivalent respecting camelCase data keys) + separators=(",", ":"), matching JSON.stringify semantics.
8. tests/test_threads.py:63 — restore the exact assertion: parse created_at, round-trip to ISO (JS toISOString format: milliseconds + Z), assert equality with the original string — not truthiness.
9. [minor] tests/test_intake_message_materialization.py:184 — `isinstance(value, int)` accepts bool; use `type(value) is int` (or exclude bool) to match `typeof === "number" && Number.isInteger`.
10. PORT_STATUS.md — after fixing: keep validate.py row ☑ only if ruling 2 is fully applied (else ◐ with note); re-verify every Wave 3 row honestly; tick the Wave 3 test rows' gate cells (lines ~109–122) once the gate passes.

GATE: cd packages/lhc-py && uv run python scripts/check_gate.py → GATE PASS, wrong=0, collection clean.

FINAL REPORT: per-finding status, gate output verbatim, any finding you dispute with the TS line justifying it.
