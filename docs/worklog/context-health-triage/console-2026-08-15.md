# Console context triage — 2026-08-15

Issue: `long-horizon-context-5ma`

## Scope

Agent: Hermes Console  
Profile: `~/.hermes/profiles/console`  
Thread file: `~/.hermes/profiles/console/lhc/threads/20260809_223837_87ebc7.sqlite`

No raw SQL writes, service restarts, or agent-record edits were used for this snapshot.

## Direct read-only snapshot at 2026-08-15T21:45Z

- SQLite integrity: `ok`
- Schema `user_version`: 6
- Events: 15,171
- Non-deleted turns: 474 (473 closed, one open)
- Active work items: 0
- Current view: `v15078`
- View created: `2026-08-15T21:39:05.340Z`
- Compact point: event 14,952
- View profile: lower bound 180,000; bands 25/25/25/25

Stored band rows:

| Band | Tokens | Characters |
|---|---:|---:|
| brief | 42,415 | 169,619 |
| detailed | 38,204 | 182,100 |
| smooth | 44,479 | 154,541 |

The full/verbatim share and live tail are not stored as a fourth `thread_view_band` row.

## Current arrangement

- Arrangement entries: 115
- Non-empty degraded fallbacks: 27
- Empty gap entries: 4

The 27 degraded brief entries are `stored_member_concat` for chunks `c22–c46`, `c60`, and `c61`. They preserve source material at lower quality.

The four empty detailed-band gaps are:

- `t179`
- `t226`
- `t231`
- `t235`

These subjects are structurally accounted for but have empty rendered bodies. They must not be counted as semantic coverage merely because an arrangement row exists.

A direct content comparison then checked every non-deleted message block in these four turns against blocks in other non-deleted turns:

| Turn | Live blocks | Exact duplicates outside the turn | Unique blocks |
|---|---:|---:|---:|
| `t179` | 1 | 1 | 0 |
| `t226` | 14 | 14 | 0 |
| `t231` | 13 | 13 | 0 |
| `t235` | 13 | 13 | 0 |

The empty gaps therefore omit no unique live block content in this thread. They are historical replay-recapture duplicates. This is a thread-specific redundancy finding, not a general rule that gap entries are harmless.

## Missing expected boundaries

- `c22–c46`: `chunk_summary_brief` absent; detailed summary ready.
- `c60–c61`: both `chunk_summary_detailed` and `chunk_summary_brief` absent.

These are absent rows, not current failed or queued jobs. Public `deriveBriefChunk`/`deriveDetailedChunk` operations currently refuse before creating version-1 boundaries in TypeScript, Rust, and Python. Lower-level immediate-claim machinery already supports an expected-missing boundary, so the missing capability is a shared SDK contract gap.

## Current failures

| Subject | Derivation | State | Version | Reason |
|---|---|---|---:|---|
| `t317` | `detailed_turn_compression` | failed | 1 | `claim_expired` |
| `t319` | `detailed_turn_compression` | failed | 1 | `claim_expired` |

These rows can be retried through the existing public turn derive operation after backup and host coordination.

## Prior bounded experiment confirmed

- `c21` brief rederivation succeeded through the public Python SDK.
- It advanced from source version 1 to 2.
- Current content length is 1,031 characters.
- Provenance is `openai-codex / gpt-5.6-luna / chunk-brief-v3`.
- Calling the same public operation for missing `c22` failed safely with `turn_not_found`; no row, work item, or inference call was created.

## Bounded repairs completed

Console used public `sdk.turns.derive_turn()` for the two existing failed boundaries. It took a mode-0600 SQLite backup immediately before each operation.

| Turn | Before | Public operation result | After |
|---|---|---|---|
| `t317` | detailed compression failed v1, `claim_expired` | `TurnDerived`, source version 2 | ready v2, 471 characters, Luna, `in_range` |
| `t319` | detailed compression failed v1, `claim_expired` | `TurnDerived`, source version 2 | ready v2, 12,916 characters, Luna, `under_min` |

`derive_turn()` refreshed the complete public turn-owned set. `turn_rendering` and `pre_detailed_assembly` also advanced from version 1 to version 2 for both turns. This was expected SDK behavior, but it matters when estimating repair scope.

Backups:

- `~/.hermes/profiles/console/lhc/repair-backups/20260809_223837_87ebc7-t317-20260815T214641.896565Z.sqlite`
  - SHA-256: `12ef3824f18e98e2e3f4c593ff1dc2e968eaacdc793e7d0edf044e9dac71e80f`
- `~/.hermes/profiles/console/lhc/repair-backups/20260809_223837_87ebc7-t319-20260815T214647.182219Z.sqlite`
  - SHA-256: `60cc1dcfca5d432573f3f44a5b93d2eea53d4c293473929581c1ecac3502ef45`

Independent verification confirmed:

- Both backup hashes match the repair receipt.
- Both backups have mode `0600` and pass `PRAGMA integrity_check`.
- Current `PRAGMA integrity_check`: `ok`.
- Current `PRAGMA foreign_key_check`: empty.
- All six target turn-owned derivations are ready at version 2.
- No derivation row is currently failed or blocked.
- No queued or claimed work remains.
- The stored view is still `v15078` at compact point 14,952.

The view did not rematerialize as a side effect. Its stored `source_state_json` still reflects the derivation counts at view creation, including the two then-failed rows. This proves that `thread_view.source_state_json` is historical view-build evidence, not current thread health. Live health must query current derivation and work rows.

## Remaining state

- Twenty-seven closed chunks still lack brief boundaries; `c60` and `c61` also lack detailed boundaries.
- Their current fallback content remains non-empty.
- The shared TypeScript/Rust/Python public chunk operations cannot yet establish a missing version-1 boundary.
- The four empty gap turns remain structurally present. Direct duplicate comparison established that they contain no unique live message blocks in this thread, so no unique live content is omitted by those gaps.
- A later normal compact can consume the repaired turn artifacts. No compact or service restart was forced during this repair.
