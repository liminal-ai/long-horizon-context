# lhc-rs Phase 2 completion audit — ACCEPTED

## Result and full-project position

Phase 2 of 3, units 9–15 of approximately 18, is **ACCEPTED** as a certified,
host-agnostic, in-process Rust library. This does not deliver LHC inside Grok
Build. Phase 3 remains approximately three integration chunks and is the
larger user-facing work.

Accepted range: Phase 1 gate `12830b3` through Wave 7 `1129bd8`.

- Sol `20260725-065705-6e2055`: **PHASE 2 ACCEPTED**
- Copilot-Fable `20260725-065705-520fa3`: **PHASE 2 ACCEPTED**

## Final gate

Both reviewers and the orchestrator ran default and
`RUST_TEST_THREADS=1` modes:

```text
exact-todo scanner self-test: PASS (13/13)
exact-todo: tokens=0 bodies=0 covered=0
classified=496 cargo-reported=496 (binaries: 58)
passed=481 suspicious=0 notimpl=0 wrong=0 ignored=15
GATE PASS
```

The transitional allowlist is absent. Reintroducing a nonempty allowlist in
final mode turned the gate RED; duplicate parsing and all todo/serialization/
storage/reconciliation tripwires remained load-bearing.

## Completion evidence

- Seven Phase 2 wave commits are present and independently certified:
  `69a7029`, `f1326d7`, `04e9050`, `2cd671f`, `81553fc`, `a0434bc`,
  `1129bd8`.
- Zero real Phase-2 todo bodies; exactly 15 Rust ignores map one-for-one to
  the 15 TS `it.skip` cases. The only exclusions remain live-network
  `inference-real` and `openrouter-call`.
- Prompt renders, JS-JSON, Node date parsing, derivation JSON order, and
  Amendment I profile-number fixtures regenerated byte-identically; all 15
  goldens matched.
- Root SDK census is 132 named exports plus seven namespaces; shared-tech
  census is 126. No public sync seam, public `Scheduler: Clone`, host
  dependency, C ABI, or subprocess inference was introduced.
- Persisted schema/migrations, SQLite/JSON order and number spelling,
  `StatementRunResult.changes`, ISO milliseconds, fractional profiles,
  `2^63`, non-finite/null propagation, and all producer paths were
  probe/mutation-verified.
- Borrowed DB ownership/no UB, rollback/close, lock freedom, scheduler
  wake/cancellation, instance isolation, read-only validation, live abort,
  concurrent derive/compact/drain, strict integer domains, and lexical
  `path.resolve` were audited.
- SDK logging polling panics return the exact structured storage-failure
  prefixes. Amendment J's Cargo example passed real success, failure, panic,
  and two-process claim paths; removing its wrapper turned the target RED.
- Ordinary path-dependency Cargo consumption compiled. The separate
  `phase3-grok-build-integration-brief.md` is present.

## Accepted non-blocking observations

The fractional inspect composition and `2^63` classifier boundary are
mutation-proven, though neither TS nor the frozen 496-case Rust inventory has
a dedicated permanent regression row. The ledger's historical SHM sentence
was clarified; the controlling ruling and implementation never copy SHM.

All audit scratch was removed. The four root `cc-lhc-*.txt` files remain
untouched and excluded.
