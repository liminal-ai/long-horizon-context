{
  "name": "pi-lhc",
  "version": "0.0.0",
  "private": true,
  "description": "PI ↔ LHC connector extension (Epic 1: Connector Core, observe-only).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "pnpm run clean && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json",
    "lint": "biome check packages/pi-lhc",
    "boundaries": "node scripts/check-boundaries.mjs",
    "test": "vitest run",
    "red-verify": "pnpm run build && pnpm run typecheck && pnpm run lint && pnpm run boundaries",
    "verify": "pnpm run red-verify && vitest run",
    "green-verify": "pnpm run verify && node scripts/check-test-immutability.mjs",
    "verify-all": "pnpm run verify",
    "format": "biome format --write packages/pi-lhc"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "lhc": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.12.4",
    "typescript": "^5.9.3",
    "vitest": "^4.1.8"
  }
}
