{
  "name": "codex-lhc",
  "version": "0.0.0",
  "private": true,
  "description": "Codex CLI ↔ LHC wrapper (PTY passthrough).",
  "type": "module",
  "bin": {
    "codex-lhc": "./dist/bin.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "pnpm run clean && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json",
    "test": "vitest run",
    "test:all": "pnpm run test"
  },
  "engines": {
    "node": ">=24.17.0 <25"
  },
  "dependencies": {
    "@lydell/node-pty": "^1.2.0-beta.12",
    "lhc": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.12.4",
    "typescript": "^5.9.3",
    "vitest": "^4.1.8"
  }
}
