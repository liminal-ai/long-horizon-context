{
  "name": "codex-lhc",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "biome format --write .",
    "lint": "biome check .",
    "format:check": "biome format .",
    "test": "echo \"No tests yet\"",
    "test:all": "pnpm run test",
    "verify": "pnpm run format:check && pnpm run lint && pnpm run test",
    "verify:all": "pnpm run verify"
  },
  "engines": {
    "node": ">=24.17.0 <25"
  }
}
