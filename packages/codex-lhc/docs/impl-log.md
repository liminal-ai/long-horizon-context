# codex-lhc implementation log

Orchestration log. Newest entries at the bottom. Each entry: what was launched/done, why, outcome, and the next action. A fresh orchestrator session resumes from `implementation-plan.md` + this file.

Subagent tooling: `cursor-subagent` (Composer 2.5, easy/moderate slices), `codex-subagent` (GPT-5.5 high: `-m gpt-5.5 -c model_reasoning_effort=high`, hard slices + pedantic verification), Agent-tool fork/fresh Fable (hardest slices, verification with cached onboarding). Monitoring: 30–60 s checks until a launched agent is confirmed working, then ~5 min intervals.

---

## 2026-07-07

**Pre-plan research (this session, before this log existed):**
- GPT-5.5-high (codex-subagent run `20260707-070902-3aaddc`) investigated `/Users/leemoore/code/.older/cxs-cloner` + real rollouts → `docs/codex-rollout-format-report.md`.
- Fork ran live synthetic-resume experiments — all passed; findings folded into the plan ("Research findings"). Experiment files left in `~/.codex/sessions/2026/07/07/` (`lhc-experiment-*`).
- Explore agent produced the cc-lhc reuse inventory — folded into the plan (category a/b/c lists).

**Plan written** → `docs/implementation-plan.md`. Phase 0 starting.

**Slice 0.1 launched** — cursor-subagent (Composer 2.5) run `20260707-074414-37e1e5`, brief in session scratchpad (`slice-0.1-brief.md`). Why Composer: mechanical copy/scaffold, easy tier. First working-check at ~45 s, then 5-min cadence.

**Slice 0.1 DONE + verified.** Composer completed: package scaffolded, 9 test files / 78 tests green, typecheck clean. Orchestrator verified inline (diff of identical-copy claims vs cc-lhc originals, re-ran typecheck+tests — all pass). Justified deviations from brief: `intake/map.ts` copied (claude version, TODO-marked) because `replay-dedupe.ts` imports `mapRolloutLine`; `fake-claude.mjs` copied for inference-lane tests. Both fine — 1.1 replaces map.ts body keeping the export surface.

**Slice 1.1 launched** — codex-subagent GPT-5.5 high, run `20260707-074820-38f99c` (mapper + turn-signal, brief `slice-1.1-brief.md` amended for the existing map.ts). Why GPT-5.5: semantic core, pedantic format work.
**Slice 1.2 launched** — cursor-subagent Composer, run `20260707-074822-08204d` (discovery + envelope, brief `slice-1.2-brief.md`). Why Composer: easy tier.
**Slice 1.3 queued behind 1.2** (brief ready: `slice-1.3-brief.md`) — not parallel because two cursor runs in one cwd can serialize/hang.

**Slice 1.2 DONE.** Composer delivered `rollout/discover.ts` + `rollout/envelope.ts`, 13 new tests green (91 total). Sane deviations: no cwd param (codex date-dirs are global), returns `{path, sessionId}`, envelope split into sync parse + async read. Orchestrator accepted on report; correctness rides the 1.4 integration + milestone live run.

**Slice 1.3 launched** — cursor-subagent Composer, run `20260707-075021-6cfc56` (lineage-db + argv, brief `slice-1.3-brief.md`).

**Slice 1.3 DONE.** Composer delivered `intake/argv.ts` (resume-intent parsing: resume-id / resume-last / none) + `intake/lineage-db.ts` (codex_* tables, resolveCaptureThread precedence: current session → resume-id → resume-last → new thread). 21 new tests, 112 total green. Deviations all codex-appropriate (subcommand parsing, global sessions root); structure parallel to cc-lhc for 1.4.

**Slice 1.1 implementation DONE (unverified).** GPT-5.5 delivered `intake/map.ts` (replaces claude copy; exports `CODEX_HARNESS_ID`, `mapRolloutLine(s)`, idempotency helpers) + `intake/turn-signal.ts` (classifier). 129 tests total green. Two self-flagged deviations to scrutinize: fingerprint fallback on parsed-object serialization (not raw bytes), and a legacy bare `type:"user"` shim kept for copied replay-dedupe tests (possible claude leakage).

**1.1 verification launched** — self-clone fork (context-cached onboarding). Scope: the two deviations, format-report conformance, turn-signal double-close hazard, fixture sanitization, plus a real-rollout smoke run (map a few hundred real lines read-only, assert zero throws and stable idempotency keys across a double pass). 1.4 brief ready (`slice-1.4-brief.md`) but held until this verdict.

**1.1 verification DONE — PASS with one CONCERN, fixed inline.** Fork verdicts: fingerprint determinism PASS (dead array branch noted); format conformance PASS; turn-signal PASS (premise corrected: cc pattern folds signals into a `turnOpen` boolean gating prune/compact — no turn_end emission from capture; codex classifier is drop-in); fixtures PASS; real-rollout smoke PASS (568 real lines, 0 throws, 0 idempotency instability across double pass, 162/162 tool call/result pairing). CONCERN: legacy claude-shaped `type:"user"` shim in map.ts — removed inline by orchestrator (small-fix rule), replay-dedupe test re-shaped to codex lines + harness literal → "codex", stableStringify array branch fixed (arrays now serialize as arrays; safe — no persisted keys yet). 129 tests green after fixes. 1.4 integration note recorded: per-line streaming must preserve `mapRolloutLines`' embedded-session_meta seen-flag accounting.

**Slice 1.4 launched** — cursor-subagent Composer, run `20260707-080112-cf7cf4` (wrapper run.ts + session.ts orchestrator + real bin.ts + fake-codex stub; brief `slice-1.4-brief.md`).

**Control-surface update from Lee:** cc-lhc modal partially works but interferes with Claude Code rendering; cc-lhc is pivoting to a "new screen" approach. Plan updated: slice 2.1 HELD until Lee points at the landed mechanism; the Phase-0 `wrapper/modal.ts` copy may be replaced wholesale (don't build on it); Phase 2 order becomes 2.2 → 2.3 → 2.4, with 2.1 whenever the mechanism lands.

**Slice 1.4 implementation DONE (unverified).** Composer delivered `wrapper/run.ts`, `intake/session.ts`, real `bin.ts` (flag strip + auto-compact suppression `-c model_auto_compact_token_limit=100000000` appended unless overridden/opted-out), `shared/codex-bin.ts`, `fake-codex.mjs` stub. 147 tests green (17 files); no interface mismatches with 1.1–1.3. E2E stub tests: capture into thread, exit-code propagation, stats line, --no-capture, resume replay-skip.

**1.4 verification launched** — codex-subagent GPT-5.5 high, run `20260707-080852-fbabcb` (brief `slice-1.4-verify-brief.md`). Focus: port fidelity vs cc-lhc lessons, `-c` argv placement validity for clap subcommands, embedded-session_meta accounting (1.1 integration note), lifecycle races, test honesty.

**1.4 verification DONE — NO-GO, 5 findings.** GPT-5.5 verdict: port fidelity PASS; auto-compact argv FAIL (override detection misses `--config k=v`, `--config=k=v`, `-ckey=v` forms); capture loop CONCERN (embedded session_meta seen-flag lost in per-line streaming); lifecycle FAIL (HIGH: child spawns before `startedAt` is stamped → fast rollout filtered out by discovery forever); tests CONCERN (e2e asserts logs, not durable thread content). Ruling: fix 1 minimum, 2+3 before trusting live resume/auto-compact.

**1.4 fix round launched** — resumed the implementing Composer session (`--resume 9a259b7d…`, new run `20260707-081253-6b3d5d`, brief `slice-1.4-fixes-brief.md`): all 5 findings, incl. deepened e2e (read thread back via SDK; resume run asserts no duplicate messages).

**1.4 fix round DONE.** Composer (resumed session) fixed all 5 findings: startedAt now stamped before spawn (+ ordering-pinning test); override detection extracted to `bin/prepare-child-argv.ts` covering all clap config forms; argv resume parsing made flag-tolerant (ambiguous → none); embedded-session_meta flag maintained across batches in session.ts + `embedded_session_meta` stat; e2e tests now read the thread back via SDK (message content + no-duplicates-on-resume). Orchestrator spot-checked both HIGHs in code. 162 green.

**PHASE 1 MILESTONE ACHIEVED (2026-07-07).** Orchestrator ran real `codex exec` under the built wrapper (`node dist/bin.js exec … -c 'sandbox_mode="read-only"'`): capture stats `lines=12 events=3 … thread=th_aa6eb1572b3da2f5`; SDK inspect.overview confirms 3 messages (2 user_prompt + 1 assistant_text incl. exact MILESTONE-OK text), 1 closed + 1 open turn, 1 chunk, 5/5 derivations ready (live claude-p inference lane worked), 0 failures. Interactive TUI pass = Lee's, pending.

**Two orchestrator inline fixes post-milestone (165 tests green):**
1. Test-isolation leak: session.test.ts created a real thread in `~/.codex-lhc` (found `session-stop-test` row in real registry). Added file-wide `CODEX_LHC_HOME` temp-dir beforeEach/afterEach (run.test.ts already did this); cleaned the leaked row/thread file/lineage.
2. Capture-quality: codex ships AGENTS.md/user_instructions/environment_context bootstrap as USER-role messages → was captured as `user_prompt` (would hit smoothing + user lane). map.ts now classifies bootstrap-prefixed user messages (`# AGENTS.md instructions`, `<user_instructions>`, `<environment_context>`, `<INSTRUCTIONS>`) as `runtime_note`; 3 new tests incl. ordinary-prompt-mentioning-AGENTS.md stays user_prompt.

**Slice 2.2 launched** — Fable fork (context-cached: format report + experiments + plan). Deliverables: `rollout/rebuild.ts` + `write-rebuilt.ts` (codex line schema, both id fields, forked_from_id provenance, event layer, `[runtime note]` swap receipt with mapper round-trip test, `replayedPrefixLines` for 2.3), plus a LIVE proof: rebuild the Phase-1 milestone thread and `codex exec resume` it with a context-recall probe.

**Slice 2.2 implementation DONE (unverified) + LIVE-PROVEN.** Fable fork delivered `rollout/rebuild.ts` + `write-rebuilt.ts`: codex line schema (both id fields, forked_from_id provenance, event twins, `[runtime note]` receipt with mapper round-trip test), temp+fsync+rename, no sqlite/index writes. 177 tests green (12 new). Deliberate deviation ACCEPTED by orchestrator: `replayedPrefixLines` EXCLUDES the receipt (receipt = new history that re-intakes; matches cc-lhc semantics — the brief's rule 6 was wrong). Live proof: rebuilt milestone thread → `rollout-2026-07-07T08-28-54-38c40c02-….jsonl`, `codex exec resume` answered "MILESTONE-OK" — rebuilt history reached the model on genuine resume. Known debt: `rollout/types.ts` still claude-shaped (TODO).

**2.2 verification launched** — codex-subagent GPT-5.5 high, run `20260707-083022-195971` (brief `slice-2.2-verify-brief.md`): field-level schema conformance incl. the live artifact line-by-line, prefix-count adjudication (off-by-one = corruption), forbidden emissions, view-entry coverage.

**2.2 verification DONE — PASS on all six items, no blocking findings.** Live artifact validated line-by-line (codex appended its resume turn in place after the 8-line rebuild); prefix adjudication: exclusion correct, hard-skip exactly the rebuilt-history lines, receipt pair re-intakes. Non-blocking notes: rebuilt files omit turn_context/task lifecycle (minimal shape proven fine); base_instructions copied without type-filtering (malformed source would propagate). Ruling: proceed to 2.3.

**Slice 2.3 launched** — codex-subagent GPT-5.5 high, run `20260707-083421-f53309` (brief `slice-2.3-brief.md`): `wrapper/session-swap.ts` (rebuild → stop capture → SIGTERM/KILL child → respawn `codex resume <newId>` → growth-confirm → lineage → capture handoff with prefix hard-skip), run.ts respawn restructure with swap-vs-real-exit discrimination, failure recovery = respawn original session id (its rollout is untouched and codex-resumable).

**Slice 2.3 DONE + verified.** GPT-5.5 delivered `wrapper/session-swap.ts` (5-state machine: rebuild → stop_old → spawn_new → confirm-by-growth → success/failure_recovery incl. respawn-original) + run.ts restructure (`spawnChild`, swap-kill discrimination); no session.ts changes needed; 183 tests green. Fork verification: PASS on handoff exactness (SDK-read-proven receipt re-intake, exact prefix equality), exit semantics, recovery honesty, cc-lessons, test honesty. Findings: MED-HIGH integration trap — `markSwapKill` contract implicit, 2.4 adapter MUST mark before `executeSessionSwap` (written into 2.4 brief as hard requirement + test); MED — growth-confirm unproven for INTERACTIVE TUI resume (probe at live milestone; fallback signal if TUI writes nothing until first input); LOW — dead-PTY stdin window, recovery linesSeen inflation.

**Slice 2.4 launched** — cursor-subagent Composer, run `20260707-084509-6b0734` (brief `slice-2.4-brief.md`): compact/prune/status commands on the swap rails, turn-open gate, no-op-prune-no-swap, plain-data command ctx (2.1 will construct the same ctx), INTERIM SIGUSR2 trigger (env-selectable compact/prune/status, marked for removal at 2.1), hard requirements from 2.3 verification baked in.

**Slice 2.4 DONE.** Composer delivered `commands/context.ts` (plain-data `LhcCommandCtx` — the seam 2.1 will construct), `compact.ts`/`prune.ts`/`status.ts`, and `wrapper/interim-trigger.ts` (SIGUSR2, env-selectable `CODEX_LHC_SIGUSR2=compact|prune|status|off`, single-flight, marked INTERIM for 2.1 removal). Hard reqs honored + tested: markSwapKill BEFORE executeSessionSwap, recovery_failed exit-code propagation, wrapper survives mid-swap child death, stdin guarded on live child. Turn-open gate sourced from `CaptureSession.isTurnOpen()` (rollout-tail classifyTurnSignal). No-op prune doesn't swap. 208 tests green (25 files).

**Phase 2 LIVE milestone launched** — orchestrator driving a scripted interactive TUI run (`milestone-driver.sh`, background): wrapper with FIFO stdin in repo cwd, prompt SWAPTEST-ALPHA → wait turn-complete → SIGUSR2 compact (rebuild+swap) → prompt SWAPTEST-BETA on the swapped session → teardown. Explicit probe: does INTERACTIVE `codex resume` touch the rebuilt rollout promptly (2.3 finding 2 — growth-confirm signal validity)?

**LIVE milestone runs (2 attempts) — swap pipeline works, confirm logic wrong for interactive.** Driver v1: bare `\r` doesn't submit in codex TUI (kitty keyboard protocol) — prompts never sent; fixed with `ESC[13u` Enter in driver v2. Driver v2 (thread `th_960035788460c523`): turn 1 captured (12 lines/3 events); SIGUSR2 → status → compact → rebuild → respawn all ran; TUI PROOF the swap actually succeeded — respawned `codex resume <rebuilt-id>` rendered "Thread forked from <orig>", the replayed SWAPTEST-ALPHA exchange, and the `[runtime note]` compact receipt natively. BUT: (bug A, = 2.3-verification finding 2 confirmed live) interactive TUI resume never touches the rollout file until new activity → growth-confirm timed out → working swap ruled failed → recovery. (bug B) recovery's kill of the not-confirmed new child was UNMARKED → its exit propagated → wrapper tore down mid-recovery. Recovery capture itself had worked (skipped_replay=3, same thread). Also noted: receipt says "serving tokens 0 -> 0" (zoneTokens on a tiny thread; cosmetic), "no bands" on 552-token thread (expected floor behavior).

**2.3 fix round launched** — resumed the 2.3 GPT-5.5 codex session (run `20260707-092659-5d6102`, brief `slice-2.3-fixes-brief.md`): fix 1 = confirm fallback `confirmed_by_child_alive` (alive child at window end = success; bad resume exits fast, experiment-backed), fix 2 = mark EVERY swap-machinery kill (incl. recovery path) swap-initiated; tests for both.

**2.3 fix round DONE** (GPT-5.5, resumed session): confirm = `growth | child_alive` with `confirmMode` in receipts; every swap-machinery kill marked (swap-spawned children suppressed until promoted; unmark on promotion so real exits propagate). 210 tests green.

**PHASE 2 MILESTONE ACHIEVED (2026-07-07, driver v2 re-run).** Full loop live-proven on thread `th_e4f81599478bb33e`: turn 1 (SWAPTEST-ALPHA) captured → SIGUSR2 → compact (view v3) → rebuild → swap (confirmed via child-alive; wrapper stayed up — fix 2 verified live) → capture handoff (`replayed_prefix=6` hard-skipped, no duplicate ALPHA in record) → turn 2 (SWAPTEST-BETA) on the rebuilt session captured into the SAME thread, incl. codex's post-resume bootstrap correctly classified runtime_note and a real tool_call/tool_result pair. SDK read-back: 9 messages (2 prompts, 2 assistant, 3 runtime_notes incl. the compact receipt, 1 tool pair), 2 closed turns, 9/9 derivations ready. Cosmetics noted for Phase 3: receipt "serving tokens 0 -> 0" (zoneTokens misleading on tiny thread), "drain not settled" warning on fast SIGTERM teardown (cap working as designed), driver's own REBUILT-detection xargs bug (test-harness only).

**Lee ruling (2026-07-07): control surface is settled — cc-lhc's alt-screen leader-key panel (commits 9bbce16→0f5e7c1: modal on the alternate screen, centered command panel, byte-clean restore, crash/signal-guarded leave). 2.1 UNHELD; port that.**

**Slice 2.1 launched** — cursor-subagent Composer, run `20260707-093652-1c2d47` (brief `slice-2.1-brief.md`): wholesale-replace stale Phase-0 modal.ts with current cc-lhc version + panel.ts, wire leader (`CODEX_LHC_LEADER`, default ctrl-]) → modal → 2.4 LhcCommandCtx dispatch → panel receipts; swap-from-panel dismisses screen BEFORE child kill/respawn; alt-screen guard audited across our extra exit paths (swap teardown, recovery_failed); REMOVE interim SIGUSR2 trigger per plan.

**Slice 2.1 implementation DONE (unverified).** Composer ported alt-screen modal+panel (wholesale replace of stale copy), added `commands/dispatch.ts` (routes /lhc-status|stats|compact|prune|help to 2.4 handlers via LhcCommandCtx, receipts in panel), rewired run.ts (leader → modal → hold+?1049h → dispatch → dismiss+flush; swap-from-panel dismisses BEFORE kill/respawn; post-swap receipts to stderr), removed interim SIGUSR2 trigger. 212 tests green.

**2.1 verification launched** — fork (terminal-lifecycle focus): port fidelity vs cc-lhc HEAD (four commits of review fixes must survive), alt-screen safety across swap/recovery/signal exits (never exit stuck in ?1049h, never drop held output), modal-open-vs-swap race, dispatch ctx correctness (markSwapKill/isTurnOpen/wrapperExitCode), leader-byte edge cases, test honesty.

**2.1 verification DONE — PASS on all six items, no blocking findings.** Port fidelity byte-clean (8-line diff, renames only; all four cc-lhc review-fix commits intact); alt-screen leave+flush on every exit path incl. swap/recovery; mark/unmark lifecycle sound end-to-end; dispatch ctx correct. LOW cosmetics logged for Phase 3: `prune <garbage>` → SDK error not usage hint; bare `/lhc` → unknown-hint not help; pre-existing stdin-drop in kill→respawn window.

**Live TUI panel check, attempt 1 — INVALID (orchestrator error ×2, no code fault):** (a) ran against a stale dist — never rebuilt after 2.1 landed, so the binary had no modal (leader byte passed through to codex; commands typed into the codex chat box); (b) driver typed dispatch-layer names (`/lhc-status`) but the panel vocabulary is short names (`status`/`stats`/`compact`/`prune [n]`/`help` — mapModalCommand). State machine separately proven correct by driving dist modal.js directly (leader→enter_modal, submit→execute). Re-running with fresh build + correct vocabulary (driver v3 fixed, EPSILON/ZETA probes).

**Live panel check attempt 2 — caught a REAL cross-workspace capture bug.** With correct vocabulary + fresh build, the wrapper attached to a CONCURRENT codex session from another workspace (Lee's t3code session): codex's sessions tree is global (unlike Claude Code's per-project dirs), and discovery picked newest-active-anywhere. Foreign thread captured (read-only; no damage — liminal git clean, swap never fired because the foreign session's open turn tripped the turn gate). Orchestrator fixed inline: `DiscoverDeps.expectedCwd` — discovery now envelope-reads each candidate (newest-first) and skips non-matching cwd (unreadable first line = retry next poll); `session.ts` passes capture cwd; envelope parser relaxed (originator/cli_version optional — only identity+cwd required for discovery decisions); session tests opt out via `expectedCwd: undefined`; new filter test (foreign-newer vs mine). 213 tests green; rebuilt; junk thread th_1d49ea0d70b43636 cleaned from registry/lineage/threads.

**Live panel check attempt 3 launched** (driver v3: status → dismiss → compact via panel, EPSILON/ZETA probes).

**PHASE 2 FULLY COMPLETE — live panel milestone PASSED (attempt 3, 2026-07-07).** Full user-facing flow proven on thread `th_22d850ecbec87180`: ctrl-] panel → `status` → dismiss → ctrl-] → `compact` → rebuild → **swap confirmed via child_alive** (the new confirm fallback, exactly as designed for interactive resume) → capture handoff (`replayed_prefix=6`) → SWAPTEST-ZETA turn captured into the same thread on the rebuilt session. SDK read-back: EPSILON exchange + compact receipt runtime_note + post-resume bootstrap note + ZETA exchange, no duplicates. Alt-screen balanced (2 enters / 2 leaves — driver's grep undercounted; python recount correct); wrapper alive throughout; cwd filter attached to the right session despite concurrent foreign codex sessions. Note: `codex resume` on our rebuilt file mints a fresh session id in codex's view (019f3ce9… active) with our written id as fork parent — lineage + capture unaffected.

**STATUS: Phases 0–2 COMPLETE incl. control surface. 213 tests. All three milestones live-proven (capture / SIGUSR2-compact-swap / panel-compact-swap).**
**Phase 3 started (Lee green-lit).** **Slice 3.1 launched** — Composer run `20260707-101349-3cf867` (brief `slice-3.1-brief.md`): receipt token metric (zoneTokens → serving tokens), prune-arg usage hint, bare `/lhc`→help, stdin-drop buffer across respawn, drain-exit message polish, recovery-child ownership audit. **Slice 3.2 (docs) queued behind it**: README, docs/onboard/06-host-codex-lhc.md, fixes-log item 10 closure.

**Slice 3.1 DONE + orchestrator-verified** (typecheck/220 tests/build re-run clean): serving-token receipt metric via `inspect.view` loadCost (fallback tailTokens); prune usage hint; bare `/lhc`→help; 4 KiB stdin ring buffer flushed on respawn; drain-exit message only when work genuinely pending (with count); recovery-child audit — no gap (failed-new always terminated before recovery spawn), test pinned.

**Slice 3.2 launched** — Composer run `20260707-102110-a0614f` (brief `slice-3.2-brief.md`): README (replace stub), docs/onboard/06-host-codex-lhc.md (mirror 05, code-verified anchors), fixes-log item 10 closure paragraph. Monitoring: 45 s working-check + 5-min finish/stall monitor (corrected cadence per Lee).

**Slice 3.2 DONE + spot-checked.** README (82 lines: quickstart, flags/envs, panel, warts), `docs/onboard/06-host-codex-lhc.md` (145 lines, file:line anchors verified), fixes-log item 10 **DONE (2026-07-07)** closure. Docs agent surfaced 5 impl-log-vs-code drifts and correctly resolved all code-wins (220 not 213 tests; copy-adapt not shared-core; child_alive confirm; conditional drain message; alt-screen panel landed).

## PHASE 3 COMPLETE — PROJECT WRAP (2026-07-07)

All phases done. Final state: 26 test files / 220 tests green, typecheck + build clean. Three live-proven milestones (exec capture; SIGUSR2 compact+swap; panel compact+swap). Docs complete (plan, this log, format report, README, onboard 06, fixes-log item 10 closed). NOT committed to git — Lee decides commit/branch strategy. Known deferred items: shared-core extraction (after both hosts settle), TUI in-app `/resume <id>` probe as a possible faster swap, native LHC inference lane (items 17/18), fidelity export/diff commands (pi-lhc has them; codex-lhc verifies via SDK reads today).

## OPEN DEPENDENCY (Lee, 2026-07-07, post-wrap)

cc-lhc's modal/control surface is STILL being debugged — expect follow-on fixes that must port into codex-lhc. codex-lhc's `wrapper/modal.ts` + `panel.ts` are byte-diffable ports of cc-lhc's (only leader/string renames), so each cc-lhc modal fix should port as a mechanical re-diff, NOT a redesign — keep them diffable (seam hygiene). When cc-lhc modal work lands: re-diff both files, re-run the modal/panel tests + a live panel milestone (driver v3 pattern in scratchpad: ctrl-] → status → dismiss → compact-from-panel across a swap). Watch for divergence points: codex swaps by respawn (dismiss-before-kill ordering) and has extra exit paths (swap teardown, recovery_failed) the cc-lhc alt-screen guard doesn't.

**Slice 2.3 DONE (inline completion in main session).** Added `wrapper/session-swap.ts` with rebuilt-rollout write, old-capture stop, graceful child termination, `codex resume <newSessionId>` respawn via prepared argv, rebuilt-file growth confirmation, post-confirm lineage, direct capture handoff with `replayedPrefixLines`, and original-session recovery on respawn/confirm failures. `wrapper/run.ts` child spawning is now reusable (`spawnChild`) with generation-safe swap-kill ignoring; no `intake/session.ts` changes were needed because the existing `continueCapture` + `knownRolloutPath` + `replayedPrefixLines` seams were sufficient. Extended `fake-codex.mjs` with `resume-append` mode. Verification: `pnpm --filter codex-lhc run typecheck` clean; `pnpm --filter codex-lhc run test` = 21 files / 183 tests green.

**STATUS: Phase 2 — 2.3 implemented; 2.1 HELD.**
**NEXT: 2.3 verification if desired → 2.4 commands onto swap rails → Phase 2 milestone end-to-end compact.**

**2.3 fix round DONE.** Fixed the two live-milestone bugs: interactive `codex resume <id>` now confirms by `child_alive` when the confirm window expires with no rebuilt-rollout growth but the child is still running; growth remains the primary `growth` confirm mode, and dead child remains failure/recovery. Swap child control now supports mark/unmark, run.ts marks swap-spawned children as suppressed until `session-swap` promotes the confirmed/recovery child, and every `terminateChild` kill marks before SIGTERM/SIGKILL. Added regression coverage for child-alive no-growth success, child-dead recovery, and run-level killed failed-new suppression while recovery child exit propagates. Verification: `pnpm --filter codex-lhc run typecheck` clean; focused swap/run/command tests 28 green; full `pnpm --filter codex-lhc run test` = 25 files / 210 tests green.

**STATUS: Phase 2 — 2.3 live-milestone fixes implemented; 2.1 HELD.**
**NEXT: re-run live milestone driver v2 → SDK verification across swap → Phase 3 candidates / control-surface follow-up.**
