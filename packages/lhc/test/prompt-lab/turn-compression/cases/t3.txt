{"specimen":"t3","inputTokens":3977,"min":1392,"aim":1989,"max":2585}
=== PROMPT BELOW — everything after this line is sent verbatim as one user message ===
<instructions-for-summarizing>
You condense dialogues from AI coding sessions so they can stand in for the original in a long-running conversation history. The condensed version is what the assistant will "remember" about this exchange later, so it must carry everything a future reader needs and nothing they don't.

Your job is to summarize the following dialogue between a user and an llm assistant to 20% to 30% of its original size and turn the back and forth dialog into a narrative generally 3rd person voicing of the events unfolding across the back and forth. keep it in narrative past tense voicing targetting approximately 20%-30% of original size. That comes out to around 1000 tokens total (roughly 800-1200). Retain maximal meaning despite compression. > represents the user and ⏺ represents the agent.

Write it as prose occasionally using bullets or headings as appropriate to what's being discussed or when techincal information needs to be retained. use best judgement on code blocks or fenced content as to whether to try and retain verbatim or simply create a fense and describe in parenthesis what was in the code block without reproducing based on best judgement as to how important that content or code block is
</instructions-for-summarizing>

<content-for-summarizing>
User:
Continue onboarding. Now read all of `packages/lhc/src/shared-tech` — work queue, scheduler, inference adapter, prompts, token counting, logging, storage, view config, instance seam. Every file, fully. Explain the machinery: how the durable queue works, claim/epoch fencing, how prompts are registered, how the inference adapter routes.

⏺ I'll read every remaining shared-tech file I haven't fully covered, starting with the work queue, durable work, and then moving through each subsystem.
⏺ I've now read every file in `shared-tech/`. Here's the comprehensive machinery analysis:

---

# Shared-Tech Machinery: Complete Analysis

## 1. Durable Work Queue (`work-queue/index.ts`) — 800+ lines

### The Item Lifecycle

A work item moves through: **queued → claimed → (done | retried-to-queued | failed_terminal)**

**Enqueue** — `enqueue(transaction, input)`:
- Generates a deterministic ID: `w-${sourceId}-${kind}-v${sourceVersion}`. This means re-queueing the same source at the same version generates the same ID (dedup), while a post-mutation replacement at a bumped version never collides.
- Inserts the `work_item` row with status `queued`.
- Upserts the `derivation` row(s) to `pending` state — this is the **only place** derivation rows are created. Completion only UPDATEs; it never inserts.
- Registers a post-commit hook that pokes the scheduler.

**Claim** — `claimNext(db, now, leaseDurationMs)`:
- One atomic `UPDATE...RETURNING` under `BEGIN IMMEDIATE` — no read-then-write gap.
- Only the **head row** (oldest by `rowid`) is considered. The queue is strictly FIFO — no skip-ahead.
- Claimable if: `status = 'queued' AND eligible_at <= now` OR `status = 'claimed' AND claim_expires_at <= now` (expired lease reclaim).
- On claim: `status → 'claimed'`, `claim_epoch += 1`, `claim_expires_at` set. If reclaiming an expired claimed row, `attempts += 1` (a crash is visible in the report and counts against retry budget).
- Returns `ClaimOutcome`: `"claimed"` with the item, `"empty"` (no rows), `"in_flight"` (head has live lease), `"waiting"` (head is backing off).

### Epoch Fencing

The `claim_epoch` column is the core coordination mechanism:
- Every claim increments `claim_epoch`.
- Every completion/retry/terminal operation checks `WHERE work_item_id = ? AND status = 'claimed' AND claim_epoch = ?`.
- If a lease expires and another process reclaims the item, the epoch increments. The original claimant's completion write misses (epoch mismatch) → `lost_lease`, meaning stale work is discarded silently with no data corruption.
- This makes cross-process safety rely on **durable rows alone** — no in-memory coordination needed.

### Completion — `complete(db, item, writes, derivedAt, onApplied?)`:
- Under `BEGIN IMMEDIATE`: verify ownership (epoch check), assert exact derivation writes match expected targets (via `assertExactDerivationWrites` — no missing, no extra, no duplicates), UPDATE derivation rows to `ready` with version check (`source_version = ?`), delete the work item, run `onApplied` hook, commit.
- If all derivation UPDATEs hit zero rows (source version moved since enqueue): `stale_discarded`. The mutation cascade already re-queued at the new version.
- A partial hit (some matched, some didn't): `DerivationCompletionError` — state corruption.

### Retry and Terminal Failure

**`retryClaimedItem`** — Returns item to `queued` with `attempts += 1`, `eligible_at` set to backoff time, `claimed_at/claim_expires_at` cleared. Epoch-checked.

**`failTerminal`** — UPDATE derivation rows to `failed` or `blocked`, copy `attempts` and `lastError` into derivation metadata (the queue is not an audit table — durable outcome detail lives on the derivation row), delete the work item. Version-checked so stale terminal failures don't stamp rebuilt derivations.

**`failAttempt`** — The combined retry/terminal decision: if retryable and under budget, retry with exponential backoff (`min(base × 2^attempts, cap)`). Otherwise, terminal.

### Supersede — `supersedeQueued(db, targets)`:
Deletes queued items for given (kind, sourceRef) pairs. Used by the mutation cascade to clean up obsoleted work before re-enqueueing at the next source version. Claimed items are left alone — their stale completions are caught by the version check.

### `createOrClaimImmediateWorkItem` — Synchronous derive path
For `turns.deriveTurn` and similar synchronous derive calls: creates or claims a work item in one transaction, checking derivation boundaries match expected state. If live work exists for the same source/version, returns `in_flight`. Otherwise creates the item and immediately claims it.

---

## 2. Durable Work Dispatch (`durable-work/index.ts`)

### Operation Intent
`operationIntent(kind, sourceRef)` maps work kinds to dispatch operations:
- `prompt_smoothing` / `tool_result_summary` → `"messages.derive"`
- `turn_derivation` → `"turns.deriveTurn"`
- `detailed_turn_compression` → `"turns.deriveDetailedTurnCompression"`
- `chunk_summary_detailed` → `"turns.deriveDetailedChunk"`
- `chunk_summary_brief` → `"turns.deriveBriefChunk"`

### `applyDerivationSuccess` / `applyDerivationTerminalFailure`
Centralized completion mechanics shared by both queue-driven and synchronous derive paths:
- Epoch-checked ownership verification
- `assertExactDerivationWrites` — enforces handler writes exactly match queued targets (prevents wiring bugs where a handler writes the wrong derivation)
- Version-checked UPDATE (stale writes → discard)
- `onApplied` hook runs inside the transaction for atomic follow-on work (chunk placement, summary enqueues)
- Post-commit hooks flushed after COMMIT

### `runWorkHandler` — Safe handler wrapper
Wraps handler execution in try/catch. A throwing handler (a bug by contract) returns `{ ok: false, retryable: false, reason: "handler threw: ..." }` instead of crashing the drain.

---

## 3. Scheduler (`scheduler.ts`) — 350+ lines

### Two Modes
- **Manual**: poke/touch are no-ops. Drain is only manual (`work.drain(ref)`).
- **Background**: per-thread single-flight drain with pending-flag coalescing.

### Background Scheduling Mechanics
Per-thread state: `{ running, pending, passes, waiters, wakeTimer }`.

**Poke** (post-commit nudge that work was queued):
1. Clear any pending wake timer.
2. If a drain is already running for this thread → set `pending = true` (burst coalesce: at most one further pass).
3. If not running → start `runLoop`.

**`runLoop`**:
1. Defers to next macrotask (so the committing operation's synchronous tail finishes first — the drain never contends with the transaction that scheduled it).
2. Loops: clear pending flag, increment passes, `runDrain(filePath, deps)`, repeat while pending.
3. On exit: if the last report stopped on `"waiting"` or `"in_flight"`, arm a wake timer.
4. Otherwise, flush waiters (drainSettled resolvers).

**Touch** (thread-file open announcement):
1. Learn threadId → filePath mapping.
2. On first touch per process lifetime: check for leftover live work and schedule catch-up.

**Wake Timer**:
- At most one per thread, `unref()`'d so it never keeps the process alive.
- Armed when a drain stops on retry backoff (`eligible_at`) or unexpired claim (`claim_expires_at`).
- On fire: re-enters the poke path → claimNext re-reads the head.

**`drainSettled(threadId)`**:
- Returns a Promise that resolves when no drain is running, no drain is pending, AND no wake timer is armed.
- A pending wake counts as unsettled: the retry it will fire is part of the drain cycle.

### Instance Isolation
The `InstanceSeam` + `AsyncLocalStorage` pattern means multiple SDK instances in one process each have their own scheduler. `scopeSurface` wraps every domain operation in `runWithInstanceSeam(seam, ...)` so deep poke/touch calls reach this SDK's scheduler. A manual SDK's operations run with a no-op seam → can never accidentally trigger a background SDK's drain.

---

## 4. Inference Adapter (`inference-adapter.ts`)

### The Pipeline
`createInferenceCallbacks(config)` returns the same 4-operation `InferenceCallbacks` interface that direct injection provides. Every operation follows:

1. **Look up assignment** for the derivation kind (provider, model, prompt name, thinking level, target ratios).
2. **Resolve prompt template** from `PROMPT_REGISTRY` by the assignment's prompt name.
3. **Merge target ratios** from the assignment into the prompt's render input.
4. **Render the prompt** — the template produces `{ role: "system"|"user", content: string }[]` messages.
5. **Bound input** — for tool results, `boundContent(content, maxInputChars)` truncates huge content to head+tail around a marker.
6. **Call host** via `safeCall(config.call, input, timeoutMs)` — try/catch + timeout race.
7. **Shape output** — trim whitespace; empty/whitespace-only → `empty_output` classified failure (retryable).
8. **Stamp provenance** — `{ provider, model, prompt }` from config, never from model output.

### Failure Classification (`classify.ts`)
Pure table:
| Kind | Retryable |
|------|-----------|
| `rate_limit` | ✓ |
| `timeout` | ✓ |
| `network` | ✓ |
| `empty_output` | ✓ |
| `other` | ✓ |
| `auth` | ✗ |
| `invalid_request` | ✗ |

**`safeCall`** — Wraps the host's `ModelCall` in:
1. A try/catch that classifies thrown exceptions as `{ kind: "other" }`.
2. A `Promise.race` against a timeout that classifies as `{ kind: "timeout" }`.
3. So host behavior can never crash a drain.

### Routing
Per-kind assignment controls routing: each derivation type can use a different provider/model/prompt. The 4 callback methods map to derivation types:
- `smoothPrompt` → `"smoothed_prompt"` assignment
- `summarizeToolResult` → `"tool_result_summary"` assignment
- `compressDetailedTurn` → `"detailed_turn_compression"` assignment
- `summarizeChunkBrief` → `"chunk_summary_brief"` assignment

---

## 5. Prompt Registry (`prompts/`)

### Architecture
Name-keyed registry. Each template is a module exporting `{ name: string, render(input) → messages[] }`. Version is in the name (`smoothing-v1`, `tool-result-v2`). Config selects by name; dial-in swaps add a module and edit config — no handler/adapter/host changes.

### Current Templates (7 total)

**`smoothing-v1`** — 5 detailed examples showing good/bad smoothing. Rules: fix typos, normalize whitespace, soften profanity. Preserve: intent, technical details, uncertainty, fenced code.

**`tool-result-v1`** / **`tool-result-v2`** — v2 is the current default. Uses parsed classification facts as authoritative. Mode-specific guidance (receipt, failure, search_summary, test_summary, content_summary, diff_summary, large_log, multi_tool_summary). Excerpts raw output for large results.

**`detailed-turn-compression-v1`** / **`detailed-turn-compression-v2`** — v2 is current. Compresses user↔assistant dialogue to target ratio (30–50%). Preserves: user constraints, exact identifiers, decisions, outcomes. Removes: pleasantries, repetition, filler.

**`chunk-brief-v1`** / **`chunk-brief-v2`** — v2 is current with extensive examples (good and bad). Converts conversation into past-tense historical narration. Has one ~4000-token elaborate prompt with 5 worked examples showing good/bad compression and a detailed instruction set about converting live instructions to historical framing.

### Defaults
```
smoothed_prompt       → smoothing-v1
tool_result_summary   → tool-result-v2
detailed_turn_compression → detailed-turn-compression-v2
chunk_summary_brief   → chunk-brief-v2
```

---

## 6. Token Counting (`token-counting/index.ts`)

Lazy-initialized singleton `Tiktoken` encoder with `o200k_base` ranks (the GPT-4o tokenizer). `estimateTokens(text)` returns the exact BPE token count. Used throughout for:
- Message token estimates at intake
- Compact selection budget arithmetic
- Compression target calculation
- Status tail-token reporting
- View rendering token counts

---

## 7. Logging (`logging/`)

### Operational Log (`index.ts`)
Domain-blind log surface. `writeLog(transaction, entry)` writes to a `log` table with level, message, derivationType, subjectId, reason, floorUsed. **Fail-soft**: never throws to caller, never shares the caller's transaction (opens its own handle or registers on postCommitHook). Query by any combination of level/derivationType/subjectId/reason.

### Derivation Execution Log (`derivation-log.ts`)
Append-only execution history for inference-backed derivations. The derivation state machine is compact (4 states); this table carries the story. Events: `inference_failed`, `inference_succeeded`, `fallback_applied`, `retry_scheduled`, `terminal_failed`. Each entry carries target (subjectKind/subjectId/derivationType), eventKind, and a payload (reason, attempts, provenance, etc.). Also fail-soft.

---

## 8. Persistence (`persist.ts`)

### `createDbReadTransaction(threadRef, operation)`
1. Runs under `runWithThreadTouchSuppressed` (reads never trigger scheduler catch-up).
2. Resolves threadRef to filePath (via registry for threadId refs).
3. Opens thread database (validates, migrates).
4. `BEGIN` → read threadId → run operation → `COMMIT`.
5. Always closes db in finally.

### `createDbWriteTransaction(threadRef, operation, clock?)`
1. Resolves threadRef, opens database.
2. Reads threadId.
3. `BEGIN IMMEDIATE` → run operation (provides `DbWriteTransaction` with db, filePath, threadId, clock, postCommitHook, poke) → `COMMIT`.
4. Flushes post-commit hooks after COMMIT.
5. Always closes db in finally.

The `DbWriteTransaction` object carries everything the operation needs: the live db handle, the resolved poke target (from `resolveInstancePoke`), and a post-commit hook set.

---

## 9. Instance Seam (`context.ts`)

### AsyncLocalStorage-based SDK scoping
`seamStore = new AsyncLocalStorage<InstanceSeam>()`. The seam carries:
- `poke: (threadId) => void` — to the SDK's scheduler in background, no-op in manual
- `touch: (filePath, db) => void` — for scheduler catch-up
- `view?: ResolvedViewConfig` — this SDK's compact profiles/budgets
- `config?: ResolvedSdkConfig` — this SDK's full config

`runWithInstanceSeam(seam, operation)` — sets the seam for the operation's async context. Every `sdk.*` call is wrapped in this.

### Fallback Path
Below-SDK direct domain calls (test utilities, single-background default path) fall through to module-global slots:
- `setSchedulerPoke(poke)` / `setThreadTouch(touch)` — installed by background SDK construction
- Manual SDKs never install, so the fallback stays inert

### Touch Suppression
`runWithThreadTouchSuppressed(operation)` — wraps the operation with `touch: () => {}`. All read transactions use this so opening a thread file for a read can never trigger scheduler catch-up.

---

## 10. View Config (`view.ts`)

Pure type definitions for:
- **ViewProfile**: name + lowerBound + 4 band percentages (must sum to 100)
- **VisibilityBudgets**: maxTokens > targetTokens
- **SdkViewConfig**: user-supplied profiles, visibility, compactThreshold
- **LlmRequestContext/SessionThreadView**: model-context and PI-session shapes
- **StoredView/CompactReceipt/ViewStatus**: snapshot and operation shapes

---

## 11. Other Shared-Tech

### `storage.ts` — SQLite primitives
`openDatabase(path)` → `PRAGMA journal_mode = WAL; foreign_keys = ON; busy_timeout = 5000; synchronous = NORMAL;`. Tracks db→path mapping in a WeakMap for logging/diagnostics.

### `thread-migrate.ts` — Schema migration
Supports versions 1→2→3 (current). v1→v2 adds derivation_log table. v2→v3 renames `smooth_turn_compression` → `detailed_turn_compression` across all tables (derivation, derivation_log, log, thread_view). Also migrates queued turn_derivation work items to include `pre_detailed_assembly` targets. Every open runs the queued work migration idempotently.

### `report.ts` — Report row mapper
`reportEntryFromRow(subjectKind, row)` — shared by both message and turn report queries so they produce identical `DerivationReportEntry` shapes.

### `deterministic.ts` — Test double inference
`createDeterministicInferenceCallbacks()` — returns callbacks that produce `marker(fnv1a-digest:first-40-chars)` text. Input-sensitive: distinct inputs → distinct outputs. Used for all test fixtures where real inference isn't needed.

### `tool-result-rendering.ts` — Fallback truncation
`truncateForFallback(text)` — truncates to 500 chars with `… [truncated N chars]` marker. Used by composition for tool calls and results when derivations aren't ready.

### `inspect.ts` — Report type definitions
Shapes for `InspectOverview`, `HealthReport`, `ViewContentsReport`. Pure types, no behavior.
</content-for-summarizing>
