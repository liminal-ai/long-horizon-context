import type { CanonicalChunk, CanonicalTurn, StoreRuntime } from "../types.js";

export async function updateChunksForTurn(_runtime: StoreRuntime, _turn: CanonicalTurn): Promise<CanonicalChunk[]> {
  return [];
}
